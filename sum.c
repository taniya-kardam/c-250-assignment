#include<stdio.h>
void sum(int a,b,c,d,e)
{
   int i,sum;
  for(i=0;i<=n;i++)
   {
    sum=sum+i;
     printf("total  number of sum is=%d",sum);
   }
}
 int main()
{
   int &j,&k,&l,&m,&n;
 printf("enter the number");
 scanf("%d%d%d%d%d",&j,&k,&l,&m,&n);
  sum(&j,&k,&l,&m,&n);
}
